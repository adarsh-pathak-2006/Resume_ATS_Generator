def prompt_generate_resume(main_resume, jd):
    prompt=f"here is the prompt with {main_resume} and jd {jd}"
    return prompt

def prompt_genrate_resume_with_coverletter(main_resume, jd, company_info):
    prompt=f"here is the prompt with {main_resume}, jd:{jd}, companyinfo:{company_info}"
    return prompt